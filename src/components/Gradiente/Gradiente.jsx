import React, { Component } from 'react';
import './Gradiente.scss';

export default class Gradiente extends Component {
    render() {
        return (
            <div>
            </div>
        );
    }
}
